{
  "name": "unified-commerce-os",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "bun scripts/migrate.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
