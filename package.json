{
  "name": "unified-commerce-os",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "migrate": "bun scripts/migrate.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "admin:dev": "cd apps/admin && bun run dev",
    "storefront:dev": "cd apps/storefront && bun run dev"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
